interface CardProps {
  name: string;
  price: number;
  category: string;
  description: string;
}

interface CardState {
  name: string;
  price: number;
  category: string;
  description: string;
}
